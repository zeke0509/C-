# 原地扩容替换
1. 题目：[54. 替换数字（第八期模拟笔试）](https://kamacoder.com/problempage.php?pid=1064)
2. 思路：先原地扩容，再从后往前填
3. 具体步骤：
	（1）先把字符串`resize`到新长度
	（2）双指针，从后往前填。这样保证不会覆盖掉还没处理的字符。
# 涉及反转字符串的题目
1. 常见思路：整体反转 + 局部反转

# `KMP`算法
1. 适用题型
	（1）实现`strStr()`
	（2）找到重复子字符串的周期：`n-lps`是周期，其中`lps = next[s.size() - 1]`表示整个字符串的最长相等前后缀的长度，也就是前后重合了多长