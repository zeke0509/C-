# 字符串可以看作是一个栈
字符串拥有的方法
- 在尾部插入元素：```s.push_back(ch);  // 只能插入字符，而不能插入字符串```
- 返回字符串的最后一个字符：```s.back();   // 字符串不能为空```
- 返回字符串的第一个字符：```s.front();```
- 移除字符串的最后一个字符：```s.pop_back();  // 字符串不能为空```
- 判空：```s.empty();```
# 单调队列要使用双端队列实现。
# 做题技巧
- 只对一部分数据的顺序进行维护：优先级队列